version: '3.8'

services:
  whatsapp-bot:
    build: .
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    tty: true      # Permite la asignación de terminal
    stdin_open: true  # Mantiene el STDIN abierto